// ================================================================
// config.example.js — config.js 템플릿 (GitHub 공개용)
//
// 사용 방법:
//   1. 이 파일을 복사하여 같은 폴더에 config.js 로 저장
//   2. apiKey 값에 Gemini API 키 입력
//   3. config.js 는 .gitignore 에 포함되어 있으므로 GitHub에 올라가지 않음
// ================================================================
const GEMINI_CONFIG = {
  apiKey: 'YOUR_API_KEY_HERE',          // ← Gemini API 키
  model:  'gemini-3-flash-preview',
};
